This is a Python-written code to convert family trees in `csv` (indented, like a file tree) into Gramps `csv`, to be imported from Gramps, an OpenSource family tree creator.
An example `csv` will be supplied.
